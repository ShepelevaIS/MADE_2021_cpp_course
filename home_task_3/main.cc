#include "set_lib/set.h"
#include <iostream>
#include <vector>

int main() {
  // Set<int> set({1, 2, 3, 4});
 // iterator<int> iter = set.end()
  // for (Set<int>::iterator iter = set.begin(); iter != set.end(); ++iter) {
  //   std::cout << *iter << " ";
  // }
  // for (Set<int>::iterator iter = set.end(); iter != set.begin(); --iter) {
  //   std::cout << *iter << " ";
  // }
  return 0;
}